import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.darleanow.lostPets',
  appName: 'lost-pets',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
